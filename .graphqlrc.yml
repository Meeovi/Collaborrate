## Please review configuration and adjust it for your own project
schema: ./server/generated-schema.graphql
extensions:
  graphback:
    model: ./server/generated-schema.graphql
    ## You can add as many Graphback plugins as you wish
    ## Here we are only adding the Schema and Client plugin
    plugins:
      graphback-schema:
        format: graphql
        outputPath: ./server/generated-schema.graphql
      graphback-client:
        format: 'graphql'
        outputFile: ./server/generated-schema.graphql
      graphback-datasync:
        format: 'graphql'
        packageName: '@graphback/datasync'
        outputPath: ./server/generated-schema.graphql