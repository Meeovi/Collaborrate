version: '3'
services:
  app:
    container_name: app
    command: npm run dev
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - .:/app
      - /app/node_modules/