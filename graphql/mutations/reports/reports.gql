
query ($id: Int!) {
  createOneReports(where: {id: {_eq: $id}}) {
    id
    ip_address
    products
    quantity
    subtotal
    updated
    email
    customer
    created
    applied_coupon
  }
}
